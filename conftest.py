# conftest.py — project root
# Placing conftest.py here causes pytest to add this directory to sys.path
# automatically, so test modules can import from processors/ and utils/
# without manual sys.path manipulation.
